---
fixes:
  - |
    Use ``dwave-graphs`` instead of the deprecated ``dwave-networkx``.
upgrade:
  - |
    Rename ``minorminer.layout.dnx_layout`` to ``minorminer.layout.graph_layout``
    and deprecate it, but keep an alias for backwards compatibility.
